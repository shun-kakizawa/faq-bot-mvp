# OpenAI の API キー（必須）。https://platform.openai.com/api-keys で取得。
# このファイルを .env という名前でコピーし、実際のキーを設定してください。
# .env は .gitignore 済みのため、キーが GitHub に上がることはありません。
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# 使用するモデル（任意・未設定ならデフォルト値を使用）
# LLM_MODEL=gpt-4o-mini
# EMBED_MODEL=text-embedding-3-small
